---
title: "AI引论3-19"  
excerpt_separator: "<!--more-->"  
categories:  
  - Blog  
tags:  
  - AI引论  
  - 算法  
---	  

运算符重载第二讲
<!--more-->

测试当前复制是否满足约束。

标准搜索：状态是黑盒，任意数据形式（节点表示），
目标测试:任意函数。


CSP问题（约束满足问题。constraint satisfaction problem)：特殊搜索问题。

（前20分钟笔记有bug没记上东西）

如何解决CSP问题？  
广搜：搜索量爆炸，每层遍历完才会遍历下一层，不可能提前找到答案。  
深搜：一路走到底。不做check且层层回溯。  


改进深搜：1，一层只考虑一个变量。（变量赋值顺序不重要）  
2，每一步都判断约束满足条件。（只考虑与前序不矛盾的可能）


基本的回溯搜素：
```bash
回溯搜索= DFS + 变量顺序+ 违反约束便失败
```

改进回溯搜素  
*顺序：什么变量下一个赋值？  
每个变量先试什么值？  
*筛选：提早识别失败。  


heuristic：  
启发1
mrv（minimum remaining value）启发（最小剩余值）:  
提前选择有最多约束（最少可能）的变量进行赋值（更有可能失败）  


启发2：  
lcv（least constraining value）  
选能给剩下变量留下更多可能值的赋值方法。  
  

筛选：  
提前检查 ：去掉加到现有后会违反约束的赋值。（信息从已赋值到未赋值，筛选未赋值的值域） 
  
边的一致性：对于X的每一个剩余值，Y都有某个赋值方式使其不会违反约束
）（///////////没看懂//////////）  
反过来检查尾部每个值是否都有头部值满足。  
边一致性不能保证有解  


CSP问题与SAT（satifcation，布尔赋值满足）问题。
回溯搜素是CSP问题通解，很多CSP能转换成SAT问题。  


SAT问题：是否存在一种bool赋值组合使得所有逻辑约束都满足。  （必须是bool形式）  
        更通用，但是很长。（调用求解器，不保证更快）


Davis-Putnam-Logemann-Loveland (DPLL) 算法  
矛盾指引的子句学习（conflict-driven clause learning）算法  

Bool运算法则：  
分配律（de morgan's law）


合取范式（CNF）：字句之间用and，子句内or  
析取范式：(DNF)：子句之间用or，子句内部and  
```bash

```  
DPLL算法：  
针对CNF（合取范式，子句外是and，子句内是析取or）  
一个子句只有四种可能：  
已经满足 至少一个字符（变量）为真  
无法满足（字符都为假）  
单字符（只有一个没赋值，别的为假）
其他

*子句为假，回溯。  
*单字符传递：只剩一个字符，直接赋真值  
*布尔约束传递（BCP）：重复使用单字符传递直到无法使用。  

DPLL:类似深搜，并不能从失败学习。  
CDCL（不考！！！！！不要求！！！）：从矛盾子句学习，把遇到的矛盾转化为新的子句加进去，以实现提前终止。    
隐含图：赋值的层级顺序（由BCP推出的计算为同级，图的边上标记从哪个子句推出）  
努力多触发BCP  


总结  
• 回溯搜索是解决CSP问题的通用解法  
• 很多CSP可以用逻辑来表达，即转为了SAT问题  
• 求解用CNF表达的SAT  
• DPLL 算法（深搜+BCP）  
• CDCL 算法（DPLL+从矛盾中学会新的子句）  
