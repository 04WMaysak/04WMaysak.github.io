---
title: "CPP类与对象01"
excerpt_separator: "<!--more-->"
categories:
  - Blog
tags:
  - 程序设计实习
  - CPP
---	

第一讲：类与对象
<!--more-->

第一讲：类与对象
1. 面向对象的核心思想
抽象：从客观事物中提取共性属性（成员变量）和行为（成员函数）。

封装：将数据与操作数据的函数捆绑为“类”，隐藏内部实现，提供明确接口。

2. 类的定义与实例化
类即用户自定义类型，通过class定义，以分号结尾。

对象（类的实例）拥有独立的成员变量存储空间，成员函数共享代码。

3. 对象的使用方式
直接访问：对象名.成员名

指针访问：指针->成员名

引用访问：引用名.成员名（本质是对象的别名）

4. 引用机制
引用是变量的别名，定义时必须初始化，且一经绑定不可更改。

关键作用：

作为函数参数，避免拷贝，直接操作原变量（如swap函数）。

作为函数返回值，允许函数调用出现在赋值语句左侧（如SetValue() = 40）。

注意：引用不能绑定常量或表达式，但可绑定const引用以延长临时对象生命周期。

5. 类成员访问权限
文档中提到“类成员的访问权限”，虽未展开，但核心原则为：

public：对外可见，接口部分。

private：仅类内部可访问，实现封装。

protected：用于继承场景。

6. 函数重载
同一作用域内，函数名相同但参数列表不同（类型、个数、顺序），编译器根据实参进行匹配。

第三讲：运算符重载
1. 设计动机
让自定义类型（如复数）也能使用+、-等运算符，提升代码可读性与直观性。

本质是函数重载，运算符被转换为函数调用。

2. 两种实现方式
重载形式	参数个数	示例
普通函数	等于运算符目数	operator+(a, b)
成员函数	等于运算符目数减1	a.operator-(b)
3. 关键规则
赋值运算符=只能重载为成员函数，且必须实现深拷贝以避免浅拷贝导致的内存重复释放问题。

编译器根据操作数类型决定调用哪个重载版本。

4. 常见重载示例
算术运算符：+、-等，通常返回临时对象。

赋值运算符：需处理自身赋值、资源释放与重新分配。

流运算符：<<和>>通常重载为普通函数，返回流引用以支持链式操作。

5. 深拷贝 vs 浅拷贝
浅拷贝：仅复制指针地址，导致多个对象共享同一块动态内存，析构时重复释放。

深拷贝：在赋值时重新分配内存并复制内容，是正确管理资源的必要手段。

