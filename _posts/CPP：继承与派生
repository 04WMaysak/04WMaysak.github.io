title: "C++面向对象：继承与派生"
excerpt_separator: "<!--more-->"
categories:
•	编程
tags:
•	
________________________________________
C++类的继承，派生，复合
<!--more-->
派生：基类/父类→派生类/子类
派生：扩充与修改。可以添加新的成员，可以重写继承的成员。
继承写法：
text
class newclass : 派生方式说明符 base_class { … }
例子：
cpp
class CUndergraduateStudent : public CStudent {
private:
    int nDepartment;
public:
    bool IsThreeGood() { ... }   // 覆盖
    bool CanBaoYan() { ... }
};
派生类拥有基类的全部成员，包括：
•	private/protected/public成员变量
•	private/protected/public方法
派生类不能访问基类的private成员。
派生方式说明符：private / public（访问权限和基类相同） / protected
为什么继承？——共同的属性和方法（由此派生）+ 不同的属性和方法
特性：派生对象大小 = 基类对象的大小 + 派生类自己的成员变量的大小。（派生类对象包含基类对象，且基类对象存在新增成员变量前）
派生类定义基类同名的成员：覆盖。派生类访问时缺省访问派生类中定义的成员，若要在派生类中访问基类同名成员，使用作用域符号 ::（如 Base::j = 1;）
复合对象：
cpp
class CCircle {
    double r;
    CPoint center;
};
如何避免循环定义：
cpp
class CDog;                     // 前置声明
class CMaster {
    CDog * dogs[10];            // 编译器希望知道这个类有多大，但类的大小只有//定义后才知道，指针的大小确定，因此使用指针可以规避问题。
};
class CDog {
    CMaster * m;                // 使用指针
};
